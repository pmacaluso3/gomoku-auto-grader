{
  "name": "auto-grader-local-script",
  "version": "1.0.0",
  "description": "Usage goals:\r - set up a .env file w/ creds\r - download a folder of ungraded applicant submission zips from admin frontend\r - `node test-script.js`\r - check any failures manually & edit them in admin frontend if needed\r - `node cleanup-script.js`",
  "main": "test-script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
